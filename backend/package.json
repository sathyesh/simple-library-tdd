{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend Server for simple library",
  "main": "index.js",
  "scripts": {
    "dev": "node bin/www",
    "test": "mocha 'src/tests/**/*.spec.js' --timeout 10000 --exit",
    "lint": "eslint ./src --fix",
    "coverage": "nyc --reporter=html --reporter=text mocha 'src/tests/**/*.spec.js' --timeout 10000 --exit"
  },
  "keywords": [],
  "author": "Sathyesh Selvrajah",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.0",
    "chai": "^4.2.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "http-errors": "^1.8.0",
    "mocha": "^8.1.1",
    "mongoose": "^5.10.0",
    "nyc": "^15.1.0",
    "supertest": "^4.0.2"
  },
  "devDependencies": {
    "eslint": "^7.7.0",
    "eslint-config-airbnb-base": "^14.2.0",
    "eslint-plugin-import": "^2.22.0"
  }
}
